On this branch **feature/GPT-clone**

I will try to have a very similar interface, but obviously i won't exhaustively do every functionality, probably just the 80% that is most useful (+chatgpt interface is quite simple)
- Messages and everything should be almost identical
- You can create a new chat, it will just reset the converstation, simple
- The sidebar on the left should be static, but identical
- The options, import a file etc.. are static too
- So everything will be static (mock) except for the converstation/interaction with the bot